server {
    listen 80;
    server_name mybaicao.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name mybaicao.com;

    ssl_certificate /etc/letsencrypt/live/mybaicao.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/mybaicao.com/privkey.pem;

    location /_next/static/ {
        alias /var/www/baicao-blog-client/.next/static/;
        expires 1y;
        access_log off;
        add_header Cache-Control "public, immutable";
    }

    location /images/ {
        alias /var/www/baicao-blog-client/public/images/;
        expires 7d;
        access_log off;
    }

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
